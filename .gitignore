```
# Supabase related
supabase/.temp/
supabase/logs/

# Dependencies
node_modules/
.yarn/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.*
!.env.example

# Database
*.db
*.db-journal

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# TypeScript v1 declaration files
typings/

# System
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Temporary files
*.tmp
*~
```

Wait, I need to reconsider this. Looking at the changes again - only SQL migration files were modified in the supabase directory. No build artifacts, dependency directories, or temporary files were mentioned in the changes. The current .gitignore appears to already have some content, but the changes are just SQL files which are source code.

Actually, looking more carefully at the example format and the instruction that if only source/config files are changed (like .sql files), then output nothing. SQL migration files are source/configuration files.

(empty - output nothing)